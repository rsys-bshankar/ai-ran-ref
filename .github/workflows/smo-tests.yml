name: SMO tests

on:
  push:
    branches: [main]
    paths: ["smo/**", ".github/workflows/smo-tests.yml"]
  pull_request:
    paths: ["smo/**", ".github/workflows/smo-tests.yml"]

# Every job only checks out and runs tests: the GITHUB_TOKEN needs nothing
# beyond reading the repository.
permissions:
  contents: read

jobs:
  unit-and-integration:
    name: Unit + integration tests (SQLite)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install shared library + service dependencies
        working-directory: smo
        run: |
          pip install -e shared
          pip install fastapi "uvicorn[standard]" sqlalchemy "psycopg[binary]" httpx pydantic python-multipart jsonschema pytest defusedxml

      - name: Run each module's unit test suite
        working-directory: smo
        run: |
          set -e
          for m in onboarding rapp-mgmt ran-nf-oam aimgf mlmr mllf so-smos \
                   a1-related sme dme r1-termination nfo focom ran-analytics \
                   intent-service sa-smos mock-near-rt-ric mock-o1-adaptor; do
            echo "::group::$m"
            (cd "$m" && PYTHONPATH=.:../shared python -m pytest tests/ -v)
            echo "::endgroup::"
          done

      - name: Run the shared library's tests (R1Client's OAuth2 client flow)
        working-directory: smo/shared
        run: PYTHONPATH=. python -m pytest tests/ -v

      - name: Run cross-service integration tests
        working-directory: smo
        run: PYTHONPATH=shared python -m pytest tests_integration/ -v

  gui-bff:
    name: Operator GUI BFF tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install fastapi "uvicorn[standard]" sqlalchemy httpx pydantic python-multipart pytest

      - name: Run the BFF test suite
        working-directory: smo/gui-bff
        run: PYTHONPATH=. python -m pytest tests/ -v

  gui:
    name: Operator GUI (typecheck, Vitest, build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: smo/gui/package-lock.json

      - name: Install, typecheck, test, build
        working-directory: smo/gui
        run: |
          npm ci
          npm run typecheck
          npm test
          npx vite build

  docker-compose-config:
    name: docker-compose.yml is structurally valid
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: docker compose config
        working-directory: smo
        run: docker compose config --quiet

  migration-postgres:
    name: Consolidated migration applies to real Postgres
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: smo
          POSTGRES_PASSWORD: smo
          POSTGRES_DB: smo
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U smo"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4

      - name: Apply migrations/001_init.sql
        env:
          PGPASSWORD: smo
        run: psql -h localhost -U smo -d smo -v ON_ERROR_STOP=1 -f smo/migrations/001_init.sql

      - name: Every table from the schema actually exists
        env:
          PGPASSWORD: smo
        run: |
          count=$(psql -h localhost -U smo -d smo -t -c "select count(*) from information_schema.tables where table_schema='public'")
          echo "Tables created: $count"
          [ "$count" -ge 30 ]

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install shared library + service dependencies
        working-directory: smo
        run: |
          pip install -e shared
          pip install fastapi "uvicorn[standard]" sqlalchemy "psycopg[binary]" httpx pydantic python-multipart jsonschema defusedxml

      - name: Every ORM-declared column actually exists in the migration, with matching nullability
        working-directory: smo
        env:
          SMO_DATABASE_URL: postgresql+psycopg://smo:smo@localhost:5432/smo
        run: python scripts/check_migration_matches_models.py
